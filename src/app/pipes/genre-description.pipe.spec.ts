import { GenreDescriptionPipe } from './genre-description.pipe';

describe('GenreDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
